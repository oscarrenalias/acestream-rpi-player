[project]
name = "acestream-event-catalog"
version = "0.1.0"
description = "RSS-backed AceStream event catalog prototype"
readme = "catalog/README.md"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.28.1",
    "pyyaml>=6.0.3",
]

[tool.uv]
package = false

